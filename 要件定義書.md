# MVCアシスタント 要件定義書

- 作成日: 2026-08-19
- 対象: C++17 / Win32 MVCデスクトップアプリ
- テスト対象: Googleテストでの機能確認用
- 実行形式: `mvc_app.exe`

## 1. 目的

Todo、Memo、Log、ファイル整理、メール・日報生成を1つのWin32アプリで提供する。ユーザー操作をView、処理の振り分けをController、データ操作と業務処理をModelへ分離する。

メール・日報生成では、社内情報を外部サービスへ送信しないことを基本方針とし、ローカルAIが利用できる場合だけ `localhost` 上のOllamaへ接続する。

## 2. 対象範囲

### 2.1 Todo

- Todoの一覧表示
- Todoの追加
- Todoの編集
- Todoの削除
- `todos.json` への保存と起動時読込

### 2.2 Memo

- Memoのタイトルと本文の登録
- 本文の複数行入力
- タイトル一覧からの選択
- 選択したMemo本文の表示
- Memoの更新と削除
- `memos.json` への保存と起動時読込

### 2.3 Log

- 全機能の操作結果を `logs.json` に保存
- 成功操作と失敗操作の両方を記録
- Todo、Memo、Log、Desktop整理、Downloads整理、ファイル監視、メール生成、日報生成を記録
- 各ログに日時を付加
- 最大200件を保持
- 201件目以降は最古のログから順番に削除
- 起動時に既存ログが200件を超えている場合も古いログを削除

### 2.4 Desktop整理

- メイン画面または監視小窓から実行
- Desktop内のフォルダを `desktop_folders` 配下へ整理
- 既存の分類フォルダは再整理対象から除外
- 実行結果を画面へ表示し、操作ログを `logs.json` に保存

### 2.5 Downloads監視・整理

- 起動時にファイル監視を開始
- Downloadsへ追加されたファイル・フォルダを検知
- 拡張子またはフォルダ内容に応じてDesktop上の分類フォルダへ移動
- 監視の開始・停止を小窓から切替
- 自動整理の成功・失敗を `logs.json` に保存

### 2.6 メール生成

- メール生成ツールから本文を入力
- 入力内容を `input/input_*.txt` として保存
- 最新の入力ファイルを処理
- ローカルAIが利用可能な場合は `127.0.0.1:11434/api/generate` へ送信
- 既定モデルは `qwen2.5:3b`。`OLLAMA_MODEL` 環境変数で変更可能
- AIには以下を要求する
  - 入力の単純なコピペをしない
  - 誰が、何を、いつ、どのような状態かを整理する
  - 事実にない内容や推測を追加しない
  - 箇条書きではなく自然な段落にする
  - 上司向けの件名と丁寧な本文にする
- 結果を `output/generated_mail.txt` に保存
- AI接続失敗時は固定文へ戻さず、日本語エラーを出力

### 2.7 日報生成

- 入力先頭に `種別:日報`、`mode:daily`、または「日報」を含む場合に日報モードと判定
- ローカルAIへ日報生成を依頼
- 件名を日報とする
- 本日の実施内容、課題・懸念、明日の予定を自然な段落で含める
- 箇条書きは禁止
- 結果を `output/generated_mail.txt` に保存
- 成功・失敗を `logs.json` に保存

## 3. 画面要件

- 起動時にメイン画面を自動表示しない
- `Ctrl+Alt+S` で監視小窓を表示・非表示にする
- メイン画面のボタン
  - TodoTool
  - MemoTool
  - LogTool
  - Desktop整理
  - メール内容生成
- 監視小窓のボタン
  - TodoTool
  - MemoTool
  - LogTool
  - Desktop整理
  - メール生成
  - 監視開始/停止
  - 閉じる
- メインウィンドウを閉じてもバックグラウンド監視を継続する

## 4. セキュリティ要件

- 外部AIサービスへ社内情報を送信しない
- AI接続先はローカルホストのみとする
- APIキーをソースコードへ記載しない
- Ollama未起動時は処理を失敗として記録する
- 生成結果は自動送信せず、ユーザーが確認して利用する
- `input` と `output` は機密情報を含む可能性があるため、クラウド同期対象にしない

## 5. データ要件

| ファイル | 内容 | 形式 |
|---|---|---|
| `todos.json` | Todoデータ | JSON |
| `memos.json` | Memoのタイトル・本文 | JSON |
| `logs.json` | 操作ログ、最大200件 | JSON配列 |
| `input/input_*.txt` | メール・日報の入力 | UTF-8テキスト |
| `output/generated_mail.txt` | 最新の生成結果またはエラー | UTF-8テキスト |

## 6. Googleテスト受入項目

### 起動・画面

- [ ] アプリがビルドできる
- [ ] アプリ起動後、バックグラウンド処理が継続する
- [ ] 起動時に監視小窓が自動表示されない
- [ ] `Ctrl+Alt+S` で監視小窓を表示・非表示できる
- [ ] 画面上に文字化けした文言がない

### Todo / Memo / Log

- [ ] Todoを追加できる
- [ ] Todoを編集できる
- [ ] Todoを削除できる
- [ ] Memoのタイトルと複数行本文を保存できる
- [ ] Memo一覧選択で本文が表示される
- [ ] Memoを更新・削除できる
- [ ] Logを追加・削除できる
- [ ] 各操作が `logs.json` に記録される

### ファイル整理

- [ ] Downloadsへファイルを追加すると自動整理される
- [ ] Downloadsへフォルダを追加すると内容に応じて整理される
- [ ] 監視を停止すると自動整理されない
- [ ] Desktop整理はボタン実行時だけ行われる
- [ ] 整理結果が画面と `logs.json` に記録される

### メール / 日報

- [ ] 通常入力で上司向けの自然なメールが生成される
- [ ] 入力文の単純な連結になっていない
- [ ] 箇条書きが出力されない
- [ ] `種別:日報` で日報モードになる
- [ ] 日報に3つのセクションが含まれる
- [ ] `input` 更新で自動処理される
- [ ] AI接続失敗時にエラーが表示・記録される

### ログバッファ

- [ ] `logs.json` が最大200件に制限される
- [ ] 201件目追加時に最古ログが削除される
- [ ] 起動時に200件超の既存ログが整理される
- [ ] 削除されたログ以外の最新ログが保持される

## 7. 未実装・確認が必要な項目

- `input` と `output` の使用済みファイル自動削除は、機密情報の保全と再確認可能性に関わるため、保持期間・削除タイミングを決めてから実装する。
- 現在の実装では、生成後も入力ファイルと最新出力ファイルを保持する。
- Googleテスト前にOllama、`qwen2.5:3b`、`OLLAMA_MODEL` の実行環境を準備する。
